import { View, Text } from "react-native";
import { SafeArea } from "../../../components/utils/safe-area.component";

export const CategoriesScreen = () => {
  return (
    <SafeArea>
      <View>
        <Text>Табове с категории, които отварят продуктов листинг</Text>
      </View>
    </SafeArea>
  );
};
